{
  "name": "perlcode-scripts",
  "type": "module",
  "private": true,
  "scripts": {
    "import-paa": "bun run import-paa.ts",
    "generate-answers": "bun run generate-answers.ts",
    "generate-embeddings": "bun run generate-embeddings.ts",
    "export-for-build": "bun run export-for-build.ts",
    "publish": "bun run publish.ts",
    "test": "bun test"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "postgres": "^3.4.5",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
